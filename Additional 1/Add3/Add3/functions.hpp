#pragma once

#define N 1000

namespace el
{
    bool IsPrime(int x); // 

    bool Range(int a); //   

    void Replacement(int n, int mas[N]);//

    void Read(int& n, int mas[N]); //

    void Write(int n, int mas[N]);
}
